# HANDOFF — start here. Then `CANONICAL_MIGRATION_DIRECTIVE.md` §0z, then §3a.2

Supersedes `HANDOFF_2026-08-15.md`. No HEAD SHA is quoted: a committed file
cannot name its own commit, and a hand-maintained one is stale on arrival.
`git log -1` and `/health` are the authorities.

## ⛔⛔ READ THIS BEFORE YOU CHANGE ANYTHING — PRODUCTION IS CONSUMING

```text
ENTITY_RESOLUTION_MODE               = consume
ENTITY_RESOLUTION_CONSUME_ALLOWLIST  = 26
```

**For the first time, a model's identity judgement can move a price on live
user data.** The cohort is one user (Danny, 26). This is deliberate and was
canaried, but it means the blast radius of a resolver change is no longer zero.

**Roll back by unsetting `ENTITY_RESOLUTION_CONSUME_ALLOWLIST`.** Consumption
stops immediately, shadow recording continues, written rows are untouched.

Verify live state from outside the container — never from `render.yaml`, which
is reference-only and already disagrees with production:

```bash
curl -s https://app.tryarnie.com/health | python3 -m json.tool
```

The `identity_adoption` block answers both canary questions at once:
`recorded` (is the producer running) beside
`memory_rows_not_keyed_by_their_own_name` (is anything consuming). **Read them
together** — an empty store reports zero on both, and "nothing consumed" and
"nothing ran" have one spelling otherwise.

## WHAT IS DEPLOYED vs WHAT IS MERGED

```text
origin/main   72a6a30   (the last two commits are DOCS ONLY)
deployed      c8482c1   the consumer seam — the last commit with code in it
schema        entres001 in_sync
```

⚠ **"Deployed" and "behavioural" are different questions.** A tranche can
contain a live behaviour change that its feature flag does not gate — `61eec60`
repaired `_memory()` and moved B-1 settlement prices with
`ENTITY_RESOLUTION_MODE=off`. Never summarise a tranche as inert because its
flag is off.

## WHAT WAS PROVEN 2026-08-15, AND WHERE

Interpretation adoption is CLOSED — directive §0z has the full account.

```text
producer reachable from ordinary turns   seams 1 (log) · 2 (tool batch) · 3 (ask)
shadow is annotation-only                0 identity-keyed rows across 836
consume scoped by its OWN cohort         coordinator rollout cannot widen it
resolver does not truncate               Russian singles resolve
no false collapse                        46 identities, 0 merges
PRODUCT binds nothing                    5 rows, 0 binding
61eec60 memory rung                      live, decided a correct B-1 price
```

⭐ **THE RESULT THAT MATTERS MOST IS THE CAVEAT.** On `Сметана 5%`:
`memory_key_refused key='5'` → `key = 'smetana 5percent'`, and
`pricing.memory` went from never-consulted to a real 4 ms lookup. **But the
price did not change**, because nothing seats a candidate for Russian sour
cream (`evidence_qualified raw=8 kept=0 DIFFERENT_IDENTITY: 8`), so nothing
caches and the correct key addresses nothing.

**The identity boundary fixes ADDRESSING. CACHEABILITY is a separate, unowned
defect, and it is now what stands between the 30.4% non-English population and
an actual memory hit.** Measured: 2 of 22 QUALIFIED and 2 of 27 non-English
foods ever cached. Do not let anyone read "30.4% unlocked" into what shipped.

## FOUR THINGS OPEN, NAMED RATHER THAN LEFT

1. ⛔ **The B-1 ASK path does not consume.** A staged food settles via
   `assemble()` from the pending operation, not from a tool call, so the
   tool-input stamp never reaches it. A bare `Сметана 5%` still refuses the key.
2. ⚠ **Seam 2 is unproven on fleet traffic.**
   `structured_food_executes_natively = {allowlisted_user: true, fleet: false}`
   and user 26 IS the allowlist, so the fleet's door has unit + mutation proof
   only. One non-allowlisted shadow canary closes it.
3. ⚠ **Entity-id form is unstable across calls** — `kefir 1percent`,
   `ground turkey 96 percent lean`, `ground turkey 94 lean`. Stable per
   surface. One measured cross-language duplicate: `Листья салата` →
   `lettuce leaves` vs `Lettuce leaves` → `lettuce`.
4. ⛔ **Canonical rows cannot be corrected** through the ordinary
   interpretation path — carried to B-1.8 (§6). **Do not reopen the ownership
   firewall**; it refused an authority mismatch correctly. What is missing is a
   canonical correction path.

## YOUR TASK — GENERAL CANONICAL SETTLEMENT OWNER, A1–A10

Acceptance criteria are in directive **§3a.2** and have not changed. Read them
before writing anything; A7 and A9 exist because a coincidence nearly passed
for a proof and a producer proof was mistaken for an adoption proof.

⚠ **THIS IS A BIGGER SLICE THAN ANYTHING ON 08-15.** Plan first, then build.
The known cost is stated in §3a.2 and has not moved: `assemble()` retrieves
NOTHING by design, so a food in neither artifact nor memory is WORSE off than
legacy, which would have found a live USDA row. That is a coverage fact, not a
code defect, and it is why the slice ships behind a coverage predicate.

## THE TRAPS THAT BIT THIS SESSION — ALL OF THEM COST A RUN

1. **A run that races an edit measures neither version.** Bit TWICE. Clear
   `__pycache__`, start the suite, then keep your hands off the tree.
2. **`git add core/ tests/` silently omits `handlers/`.** A broken import was
   pushed and the suites had passed against the working tree, not the commit.
   `git add -A`, then `git diff HEAD origin/<branch>` before believing a push.
3. **An AST gate that substring-matches is a grep trap in costume.** A mutation
   defeated one by aliasing an import. Assert STRUCTURE — ancestry, nesting —
   never spelling.
4. **A metric's denominator must be the population it claims.** One reported
   `16 of 15 (106.7%)` by dividing resolutions (interpretation time) by rows
   (settle time).
5. **SQL operator precedence.** `where user_id=26 and A or B` reported three
   other users' rows as Danny's.
6. **pytest here prints NO summary line.** Count progress characters:
   `.`=passed `s`=skipped `x`=xfail `F`=FAILED. Exit code alone is not enough.
7. **Verify a mutation LANDED before reading its result.** Count occurrences
   before and after the replace.
8. **An outage produces clean, confident zeros.** Two corpus runs died on API
   credits and reported a beautiful mix over a survivor set.
   `turns_reached_the_model` is the only reason they were not published.

## COMMANDS

```bash
../arnie/.venv/bin/python -m pytest tests/ -p no:randomly -q > /tmp/sq.log 2>&1; echo $?
```

```bash
TEST_POSTGRES_URL="postgresql+psycopg://$(whoami)@localhost:5432/arnie_test" ../arnie/.venv/bin/python -m pytest tests/ -p no:randomly -q > /tmp/pg.log 2>&1; echo $?
```

⚠ `TEST_POSTGRES_URL`, **not** `TEST_DATABASE_URL` — the wrong name runs SQLite
twice while looking like a two-engine proof. The tell is an identical skip
count. Correct: **SQLite 107, Postgres 25**. ⚠ `+psycopg`, not `+asyncpg`.

Baseline on the current tree: **SQLite 9136/0 · Postgres 9218/0**.

```bash
ARNIE_PROVE_DB="postgresql+psycopg://$(whoami)@localhost:5432/arnie_migcheck" ../arnie/.venv/bin/python -m scripts.prove_the_seam_is_reachable
```

```bash
ARNIE_PROVE_DB="postgresql+psycopg://$(whoami)@localhost:5432/arnie_migcheck" ../arnie/.venv/bin/python -m scripts.prove_the_russian_single_resolves
```

```bash
../arnie/.venv/bin/python -m scripts.corpus_through_the_real_turn --validate
```

The corpus driver costs ~120 real turns per arm. **Do not spend it on
architectural reachability questions** — 4–12 preregistered turns prove a seam;
large corpus budget is for after the seam is proven reachable.

## STANDING CONSTRAINTS

Admin token via `ARNIE_ADMIN_TOKEN`, **never a literal** · prod DB URL from
`../arnie/.env` (gitignored), credentials masked · **do not modify the
interpreter prompt** — enhancements go in CODE · **no food-name lists, no regex
identity, no curated exception tables** · main AUTO-DEPLOYS · **never amend a
pushed migration** · parallel cloud sessions run — always fetch and verify SHAs
· the credential ratchet (`SCRIPTS_READING_ENV_BASELINE`, now 33) fires on the
commit that adds a script and must be raised WITH ITS REASON in that same
commit · **DO NOT REOPEN:** B-1 · B-1.5 · B-1.6 · lifecycle · locking ·
settlement · replay/idempotency · **ownership firewall**.
