# HANDOFF — 2026-08-18 (end of day) — P17 scan/binding: gate NOT cleared, canaries pending

> ⚠ **SUPERSEDED IN PART, LATER THE SAME EVENING — read this box first.** Both
> canaries were run at 21:01–21:02.
> · **TWO-TURN canary PASSED**: op 87 held snapshot 1, the chip settled bound
>   → **row 3031 is the first bound row in production** (rung `product`,
>   `product_evidence_id=1`, `off:70004199`). But it reads 55 g / 110 kcal for
>   2 bars eaten: the ask said "is each bar one 55 g serving?" and the chip
>   that lexically matched an affirmative was **1 serving** — the copy defect,
>   fixed (chips now `[110 g — 2 servings] [55 g — 1 serving]`, question names
>   the unknown and asks for the total).
> · **DIRECT canary FAILED — MISROUTED (CF5b, P1 authority violation)**: the
>   scan bound at 21:01:04, the interpreter read "2 servings of Barebells
>   bars" as a correction of legacy row 3030 (2 bar/400 already on the board)
>   → `update_food_entry(3030, "4 bar")` → every binding check keys on
>   `log_food` → the op fell to the legacy executor's ratio arm → **800 kcal
>   committed by heuristic bar-mass, snapshot silently discarded, bound
>   predicate never ran** (no `settlement_route` line). Fix built + proven
>   (see §CARRY-FORWARD REGISTER CF5b, verbatim from Danny):
>   `_lift_bound_correction_to_log` (planner) · `ScanBoundNotLegacy` backstop
>   (native stage, before the legacy claim, keyed on the BINDING) ·
>   `ScanBoundCorrectionRefused` (correction_application) ·
>   `tests/test_scan_binding_dominates_correction_claims.py`, every mutation
>   seen RED.
> · **NEXT**: deploy → Danny deletes 3030 and corrects 3031 → "2 servings"
>   in-app (a B-1.8 canonical correction on a BOUND row: `product_evidence_id`
>   must survive, 110 g / 220 kcal, a `canonical:correction` event; NOT the
>   direct canary) → **RERUN the direct canary** (scan → "2 servings of
>   Barebells" → a NEW bound row; 3030/3031 untouched; `scan_rejects_
>   correction_shape` may or may not fire depending on the interpreter — the
>   row is the proof either way). P17g stays blocked until then.

Read `docs/CANONICAL_MIGRATION_DIRECTIVE.md` §NEXT first (the ONLY board), then
§CARRY-FORWARD REGISTER (CF1–CF10), then this file. Memory index:
`~/.claude/projects/-Users-danielvoskin-Code-Learn/memory/MEMORY.md` (top four
entries are today's).

## Where we are — one paragraph

B-1.8 (canonical correction lane) is CLOSED (three production canaries).
P17 scan/binding + the iOS barcode producer + P17-UA unit-evidence (A/B/C =
CF9 bound-ask continuity) are SHIPPED and DEPLOYED: `origin/main` =
`e11f198`, live `/health` = `e11f1982fa97`, schema `produnit001` applied =
expected, in sync, lanes `structured_food,ledger_undo`, allowlist [26]. iOS
build **386** is on Danny's phone (Release) and archived for TestFlight
(`~/Library/Developer/Xcode/Archives/2026-08-18/Arnie-386-p17-barcode.xcarchive`);
arnie-ios branch `p17/barcode-transport` @ `9624153`. **THE GATE IS NOT
CLEARED: zero bound rows exist in production** (`select count(*) from
food_entries where product_evidence_id is not null` = 0). P17g is BLOCKED
until BOTH canaries below commit a bound row.

## IMMEDIATE NEXT STEP — the two production canaries (Danny runs, you read the DB)

Preconditions (verify, don't assume): `curl https://arnie.onrender.com/health`
→ commit `e11f198…`, `schema.applied == produnit001`, `in_sync true`. Danny on
iOS build 386.

1. **DIRECT** — Danny: tap scanner → scan Barebells **70004199** → the chip
   ("Barebell salty peanut protein bar · 70004199") appears above the composer
   → type `2 servings of Barebells` → send.
   Expect (read the DB, never the reply): new `food_entries` row for user 26
   with `product_evidence_id = 1` (the acquisition snapshot), `pricing_rung =
   'product'`, `calories ≈ 220`, `resolved_grams ≈ 110`,
   `conversion_evidence_ids_json` contains `off:70004199`; `turn_metrics` shows
   ONE row for the turn (native; no second `llm` row = no legacy delegation);
   Render log: `product_acquired code=70004199 snapshot=1`,
   `settlement_route decision=Supported`, no `pricing.memory` stage.
2. **TWO-TURN** — scan again → chip → type `2 barebells bars` → send. Expect
   the reply *"Got the scanned … The label gives nutrition per 55 g serving —
   is each bar one 55 g serving?"* with chips **2 servings** / **1 serving**;
   `pending_operations` gains an `awaiting_answer` row for user 26 whose
   `canonical_payload.item.product_evidence_id == 1`; ZERO food rows, ZERO
   `idempotency_records`, no legacy. Then Danny taps **2 servings** (or types
   it). Expect: the same assertions as (1) on the new row (`product_evidence_id
   = 1`, 220 kcal, `scaling_factor 1.1`), `idempotency_records` gains one
   `clarification_answer` claim, no `product_acquired` log line for the answer
   turn (no reacquisition), no `pricing.memory`, no legacy.
   ⚠ Typed-answer route: the native lane does NOT check for an owning canonical
   operation before interpreting; a typed "2 servings" reaches the interpreter
   first, should yield no op, delegate, and legacy's structured block calls
   `b1_answer_turn.handle` which settles it. Works but indirect — WATCH IT. A
   chip tap goes straight to `POST /chat/answer`.

Read helper (prod, read-only; URL from `../arnie/.env`, strip `+psycopg`):
```
../arnie/.venv/bin/python - <<'PY'
import psycopg
url=[l.split("=",1)[1].strip() for l in open("../arnie/.env") if l.startswith("DATABASE_URL=")][0].replace("+psycopg","")
with psycopg.connect(url, autocommit=True) as c:
    cur=c.cursor()
    cur.execute("select id,parsed_food_name,quantity,calories,pricing_rung,product_evidence_id,resolved_grams,scaling_factor,conversion_evidence_ids_json,timestamp from food_entries f join daily_logs d on f.daily_log_id=d.id where d.user_id=26 order by f.id desc limit 5"); print(cur.fetchall())
    cur.execute("select id,turn_id,outcome,total_ms,stages_json from turn_metrics where user_id=26 and channel='ios' order by id desc limit 6"); print(cur.fetchall())
    cur.execute("select id,status,source_turn_id,left(canonical_payload,200) from pending_operations where user_id=26 order by id desc limit 2"); print(cur.fetchall())
    cur.execute("select id,command,status,turn_id,result_entry_id from idempotency_records where user_id=26 order by id desc limit 3"); print(cur.fetchall())
PY
```

If BOTH commit a bound row → close P17g on the board (P17-CAN → ✅), then P17h,
then the frozen 232-meal remeasure (both predicate commits, 40% gate
unchanged), then P17-UE (CF10), then cohort expansion. If either fails, trace
first (conversation_logs.reasoning_json `route`, turn_metrics stages, Render
log lines `product_acquired` / `settlement_route` / `scan_bound_*` /
`native_no_plan`) — the last three canaries each found a producer-side layer
in FRONT of the proven stage; expect the same class.

## What was built today (all pushed)

| commit | what |
|---|---|
| `0ab1f2d` c2.2 · `1ebbce2` c2.3 · `b8edc11` B-1.8d | bound evidence constraint; SetPreparation; stale-undo tip guard; B-1.8 proof matrix |
| `231fcf9` `c74ae98` `7c7ce49` | canaries 1–3 findings: native planner sees the board; correction reply from receipt; undo native lane; **B-1.8 CLOSED** |
| `e8d6d5a` `90a5304` P17-SB | scan is binding: BoundUnpriceable verdict, price(bound) refuses heuristic mass, snapshot id on the row; `_backfill_city` dead-path regression |
| `7ef684d` | WS handler carried no barcode AND `_stream_turn` NameError since ba8e62a (every iOS turn fell back to REST) — fixed; module-wide free-name gate |
| iOS `23f5b2c`/`9624153` (p17/barcode-transport) | scan = staged attachment; raw code on REST body + WS frame; no on-device product prose; build 386 |
| `e7da0da` `a3b69bb` `897b7da` `7d1926b` | LC1–LC3: label's own serving is a sourced conversion; scan answers the flavor ask; bound scan interpreted cold (legacy pending prior); refusal answered in words; user's stated label unit outranks interpreter rewrite |
| `e3e5f1e` P17-UA A/B | `product_unit_evidence` (migration `produnit001`), OFF unit producer (sources 2/3), pricer alias measures; **70004199 yields NO alias** |
| `7187742` CF9 / slice C | bound ASK holds the snapshot (`core/product_bound_ask.py`); B-1 settle bound + writes receipt; `answer_from_text` no longer launders heuristic grams as USER_STATED; predicate no longer reads memory for bound items |
| `e11f198` | directive reconciled: sequence, current-state table, CF10/§P17-UE verbatim |

## Standing decisions (Danny, today — do not relitigate)

- Option 1: **never translate bar → serving.** Refusal/ask is preferable to a
  silent CF4 violation. Mechanical single-serve rule ONLY when evidence states
  `product_quantity == serving_quantity` AND `package_count == 1` — never to
  rescue 70004199 (fact absent).
- The bound ROW — not the ask — clears P17g. Canary #4 proves machinery; the
  two-turn canary proves the natural journey. BOTH required.
- No API-token canary (would prove ingestion, not the producer). CF6.
- P17-UE (CF10) begins only after P17g/h + the remeasure; must finish before
  cohort expansion; never alters frozen population/predicate/40%/attribution.
- Semantic object of an option is the PATCH (quantity 2 × unit serving), never
  the label string.

## Open / owed (register has the authoritative list)

- CF9 refinement: the "yes"/"2 servings" confirmation is NOT persisted as
  `user_confirmed` unit evidence (needs `produnit002`: confirmation keyed to
  the operation before the entry exists + answer semantics). Receipt still
  names snapshot + panel conversion.
- CF2 second half: `PricingRefused` at settle still takes the generic
  recovery floor.
- CF8: legacy narrator claims actions it did not take (phantom "cleared" /
  "logged"); legacy pending ask with a `log_date` stays open until next day.
- CF1 ufm 1941501 poisoned memory row (M1). CF3 entry 2674 unledgered.
- Native lane has no pre-interpretation check for an owning canonical
  operation (see WATCH IT above).
- Row 3030 (legacy 400-cal Barebells guess from 19:39) is on Danny's board;
  legacy row — delete via the app if it clutters the canary.
- OFF **search** breaker (`search.pl`) is open/flaky in prod; exact-barcode
  acquisition (`api/v2/product`) works.

## Recipes / traps

- Full suite: `TZ=UTC TEST_POSTGRES_URL="postgresql+psycopg://$(whoami)@localhost:5432/arnie_test" ../arnie/.venv/bin/python -m pytest tests --ignore=tests/e2e -p no:cacheprovider > log; PYTEST_EXIT=$?` — capture pytest's exit; `addopts` already has `-q` (a second `-q` hides the count line); `+asyncpg` URL breaks the shared PG harness (174 errors). Last green: **9617 passed, PYTEST_EXIT=0**.
- iOS: `xcodegen generate` BEFORE `xcodebuild` (new files aren't in the target otherwise); device builds `-configuration Release`; Swift Testing results are `✔ Test run …` lines, XCTest's "Executed 0 tests" is meaningless; sim `iPhone 16 Pro Max` id `5CE735BF-…`; phone DV `5C2F7F6F-…`.
- Deploys are MANUAL (Render); read `/health` (commit + schema) — never infer.
- The A8 AST gate: `NativeExecutionStage.run` may hold NO except handler; the A11 gate lists `decide()`'s verdict constructors BY NAME; the zero-rule gate forbids string comparison in `canonical_pricing.py`.
- Prod DB writes from this harness are blocked by the permission classifier — hand Danny a script or an in-app action.
- Recovery copy tells the failure class: "Lost the thread there" = EMPTY reply (kind `stall`); "temporarily unavailable" on iOS = the turn errored (check `turn_metrics.outcome`, now records `error:<Type>` for caught stage errors).
